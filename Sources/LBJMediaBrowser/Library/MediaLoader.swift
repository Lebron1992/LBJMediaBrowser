import Combine

/// 一个负责加载媒体的对象。
/// An object that is responsible for loading medias.
public class MediaLoader: ObservableObject {

    /// 开始加载媒体
    /// Starting loading media
  public func startLoadingMedia() {}
}
